# Template for config/secrets.yaml (gitignored). Copy this file to
# config/secrets.yaml and fill in real values before running scripts/push.py.
# The names on the left (SSIDs, site names) must match wlan_templates.yaml /
# sites.yaml exactly — push.py looks them up by name.

wlan_psks:
  BB-DC-Ops: "CHANGE_ME"
  BB-DC-IoT: "CHANGE_ME"
  BB-Corp-IoT: "CHANGE_ME"
  BB-PopUp-Staff: "CHANGE_ME"
  BB-PopUp-Guest: "CHANGE_ME"

site_vars:
  BB-Retail-Denver-0142:
    pos_psk: "CHANGE_ME"
  BB-Retail-Columbus-0210:
    pos_psk: "CHANGE_ME"
  BB-Retail-Mall-Scottsdale-0087:
    pos_psk: "CHANGE_ME"
  BB-Retail-Mall-KingOfPrussia-0311:
    pos_psk: "CHANGE_ME"
